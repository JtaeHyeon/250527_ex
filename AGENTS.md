# AGENTS.md

이 파일은 이 저장소에서 작업하는 AI 코딩 에이전트
(예: OpenCode, Cursor, Copilot, Claude 등)를 위한 행동 지침을 제공합니다.

---

## 프로젝트 개요

이 워크스페이스는 다양한 웹 및 프로그래밍 개념을 연습하고 실험하기 위한 공간입니다.
SVG 에셋, 예제 스크립트 등 다양한 학습 자료가 포함되어 있습니다.

---

## 일반 행동 지침

- **언어**: 사용자가 명시적으로 다른 언어를 요청하지 않는 한 **한국어**로 응답한다.
- **어조**: 항상 친절하고, 격려하는 태도로 응답한다.
- **응답 길이 및 상세도**:
  - 사용자의 모든 요청에 대해 **최소 100자 이상**의 응답을 제공한다.
  - 단순한 질문이나 작업에도 충분한 맥락, 설명, 근거를 함께 제공하여 사용자가 내용을 온전히 이해할 수 있도록 한다.
  - 간단한 한 줄 답변은 지양하며, 항상 도움이 되는 부가 설명을 포함한다.
- **행동 설명 원칙**: 파일 수정, 명령어 실행, 의사결정 등 어떠한 행동을 수행할 때든 다음 세 가지를 반드시 설명한다.
  1. **무엇을** 하고 있는지 (What)
  2. **왜** 그 방법을 선택했는지 (Why)
  3. **어떤 결과**를 기대할 수 있는지 (Expected Result)
  - 모든 상호작용을 학습의 기회로 삼아, 사용자가 스스로 이해하고 성장할 수 있도록 돕는다.

---

## 코드 스타일

- 들여쓰기는 일관되게 유지한다 (HTML/CSS/JS는 2칸, Python은 4칸).
- 직관적이지 않은 코드 구간에는 반드시 주석을 추가한다.
- 영리함보다 **가독성**을 우선시한다.

---

## 커밋 컨벤션

커밋 메시지는 다음 형식을 따른다.

```
<타입>: <한글로 작성한 변경 내용 요약>
```

**타입 종류**: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`, `asset`

**예시**:
```
docs: AGENTS.md 행동 지침 작성
feat: 로그인 페이지 UI 추가
fix: 버튼 클릭 이벤트 미동작 수정
asset: 고양이 SVG 아이콘 추가
```

> 상세 규칙은 `.agents/git-commit/SKILL.md`를 참고한다.

---

## 주요 명령어

> 별도 언급이 없는 한 프로젝트 루트에서 실행한다.

| 목적           | 명령어                  |
|----------------|-------------------------|
| 에디터에서 열기 | `code .`               |
| 로컬 서버 실행  | `npx serve .`          |
| Git 상태 확인  | `git status`            |
| Git 푸시       | `git push origin main`  |

---

## 프런트엔드 디자인

- 모든 프런트엔드 디자인 및 UI 구현은 **[DESIGN.md](./DESIGN.md)** 를 반드시 참고하여 진행한다.
- 이 프로젝트는 **Glacier (Glassmorphism)** 디자인 시스템을 사용한다.
- 색상, 글래스 효과, 타이포그래피, 컴포넌트 스타일 등 모든 시각적 결정은 DESIGN.md의 명세를 기준으로 한다.

---

## 주의 사항

- 빌드 아티팩트 및 OS 전용 파일(`.DS_Store`, `Thumbs.db` 등)은 커밋하지 않는다.
- SVG, 이미지 등 에셋 파일은 역할을 명확히 알 수 있는 이름으로 관리한다.